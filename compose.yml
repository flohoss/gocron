services:
  backend:
    build:
      context: .
      dockerfile: docker/Dockerfile.dev
    command: air -c .air.toml
    volumes:
      - ./:/app/
      - /var/run/docker.sock:/var/run/docker.sock
    environment:
      - TZ=Europe/Berlin
    ports:
      - 8156:8156
